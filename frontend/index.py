# Main Streamlit entry point — the full app lives at app.py in the project root.
# Run from project root: streamlit run app.py
import streamlit as st

st.info("Run the full app from the project root: `streamlit run app.py`")